# create a program that takes in the pet inventory from a world of warcraft account and prints out the name of each pet and the average selling price of that pet. Sort the pets by average selling price from highest to lowest.

import re
import pyperclip

# Create a regex for pet names
petNameRegex = re.compile(r'''''')
# Create a regex for pet prices
petPriceRegex = re.compile(r'''''')
# Create a regex for pet quantities
petQuantityRegex = re.compile(r'''''')
# Create a regex for pet levels
petLevelRegex = re.compile(r'''''')
# Create a regex for pet breeds (optional)
petBreedRegex = re.compile(r'''''')  # optional
# Create a regex for pet qualities (optional)
petQualityRegex = re.compile(r'''''')  # optional
